defmodule Scrawly.Repo.Migrations.AddGameResults do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create table(:game_results, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true

      add :player_id,
          references(:users,
            column: :id,
            name: "game_results_player_id_fkey",
            type: :uuid,
            prefix: "public"
          ),
          null: false

      add :game_id,
          references(:games,
            column: :id,
            name: "game_results_game_id_fkey",
            type: :uuid,
            prefix: "public"
          ),
          null: false

      add :room_id,
          references(:rooms,
            column: :id,
            name: "game_results_room_id_fkey",
            type: :uuid,
            prefix: "public"
          ),
          null: false

      add :score, :bigint, null: false, default: 0
      add :player_username, :text

      add :created_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")
    end
  end

  def down do
    drop constraint(:game_results, "game_results_player_id_fkey")

    drop constraint(:game_results, "game_results_game_id_fkey")

    drop constraint(:game_results, "game_results_room_id_fkey")

    drop table(:game_results)
  end
end
